{% extends 'base.html.twig' %}

{% block body %}
    <br>
    <center>
        <h1>Formation</h1>
    </center>
    <br>
    <div class="row justify-content-center">
        <div class="col-md-4 col-sm-6 p-0 truc">
            <div class="service-item text-center">
                <br>
                <h4>Titre :{{formation.titre}}</h4>
                <br>
                <h4>ID : {{formation.id}}</h4>
                <br>
            </div>
        </div>
    </div>
    <br>
    <center>
    <a class="btn btn-main" href="{{ path('formation_index') }}">Retour à la liste</a>
    <a class="btn btn-main" href="{{ path('formation_edit', {'id': formation.id}) }}">Modifier</a>
    <br>
    <br>
    {{ include('formation/_delete_form.html.twig') }}
    </center>
{% endblock %}
