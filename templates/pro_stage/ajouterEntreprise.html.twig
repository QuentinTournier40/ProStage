{% extends 'base.html.twig' %}

{% block body %}
    <br>
    <center>
    <h1>Création d'une entreprise</h1>
    </center>
    <br>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-4">
            </div>
            <div class="col-md-4">
                <div class="contact-form">
                    {{ form_start(vueFormulaireEntreprise) }}             
                        {{ form_row(vueFormulaireEntreprise.nom)}}
                        {{ form_row(vueFormulaireEntreprise.adresse) }}
                        {{ form_row(vueFormulaireEntreprise.activite) }}
                        {{ form_row(vueFormulaireEntreprise.lienSite) }}
                        <br>
                        <center> <button type="submit" class="btn btn-main">Enregistrer</button> </center>
                    {{ form_end(vueFormulaireEntreprise) }}	
                </div>
            </div>
            <div class="col-md-4">
            </div>
        </div>
    </div>
{% endblock %}