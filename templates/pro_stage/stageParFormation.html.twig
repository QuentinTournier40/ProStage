{% extends 'base.html.twig' %}

{% block body %}
    
	<br />
	<h1>
        <center>Stages pour la formation <strong>{{formation.getTitre()}}</strong> :</center>
    </h1>
	<br />

    <div class="container-fluid">
	<div class="row">
		<div class="col-md-12">
			<table class="table">
				<thead>
					<tr>
						<th class="col-md-2">
						</th>
						<th class="col-md-3">
							Nom stage
						</th>
						<th class="col-md-2">
							Nom entreprise
						</th>
						<th class="col-md-3">
							activité
						</th>
						<th class="col-md-2">
						</th>
					</tr>
				</thead>

				<tbody>
                    {% for stage in formation.getStages() %}
                        <tr>
							<td class="col-md-2">
							</td>
                            <td class="col-md-3">
                                <a href="{{path('pro_stage_detail_stage', {'id': stage.getId()}) }}" <button type="button" class="btn btn-link">{{stage.getTitre()}}</button>
                            </td>
                            <td class="col-md-2">
                                <a href="{{path('pro_stage_stage_par_entreprise', {'id': stage.getEntreprise().getId()}) }}" <button type="button" class="btn btn-link">{{stage.getEntreprise().getNom()}}</button>
                            </td>
                            <td class="col-md-3">
                                {{stage.getEntreprise().getActivite()}}
                            </td>
							<td class="col-md-2">
							</td>
                        </tr>
                    {% endfor %}
				</tbody>
			</table>
		</div>
	</div>
    </div>

{% endblock %}